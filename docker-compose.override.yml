services:
  mongo:
    volumes:
      - ./docker-entrypoint-initdb.d/db-init.dev.js:/docker-entrypoint-initdb.d/db-init.dev.js:ro

  api-server:
    volumes:
      - ./api:/app/api
      - ./lib/types:/app/lib/types
      - ./lib/api-client:/app/lib/api-client

  discord-bot:
    volumes:
      - ./discord:/app/discord
      - ./lib/types:/app/lib/types
      - ./lib/api-client:/app/lib/api-client

  twitch-bot:
    volumes:
      - ./twitch:/app/twitch
      - ./lib/types:/app/lib/types
      - ./lib/api-client:/app/lib/api-client

  runnerwatcher:
    volumes:
      - ./runnerwatcher:/app/runnerwatcher
      - ./lib/types:/app/lib/types
      - ./lib/api-client:/app/lib/api-client

  racebot:
    volumes:
      - ./racebot:/app/racebot

  ws-relay:
    volumes:
      - ./ws-relay:/app/ws-relay
      - ./lib/types:/app/lib/types
