{
  "name": "helpasaur-king",
  "version": "1.9.1",
  "repository": "git@github.com:greenham/helpasaur-king.git",
  "scripts": {
    "build": "DOCKER_BUILDKIT=1 docker compose build --force-rm",
    "build:base": "DOCKER_BUILDKIT=1 docker compose build --force-rm helpa-base",
    "start": "docker compose up -d",
    "stop": "docker compose down",
    "logs": "docker compose logs -f --tail 1000",
    "start:dev": "docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d",
    "start:dev:logs": "pnpm start:dev && pnpm logs",
    "boom:dev": "pnpm stop && pnpm build:base && pnpm build && pnpm start:dev --force-recreate && pnpm logs",
    "pull:ghcr": "VERSION=${VERSION:-latest} docker compose -f docker-compose.yml -f docker-compose.ghcr.yml pull",
    "start:ghcr": "VERSION=${VERSION:-latest} docker compose -f docker-compose.yml -f docker-compose.ghcr.yml -f docker-compose.dev.yml up -d",
    "start:ghcr:logs": "pnpm start:ghcr && pnpm logs",
    "version:bump": "bash scripts/bump-version.sh"
  },
  "author": "https://github.com/greenham",
  "license": "MIT",
  "dependencies": {
    "helpa-api-client": "file:./lib/helpa-api-client"
  }
}
